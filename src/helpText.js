module.exports = `
Switch easily between environments in your Flutter project

- Sets the app id in the files that need this.
- Writes a Dart file with a class that contains constants from the flutter-env.json file.
- Optionally switches the Firebase files.
- Writes the new environment name in the .flutter-env file.

`;